'use client';
import React from 'react';
import style from './style';

export default () => {
    const { IconGroupe, Icon } = style();
    return (
        <IconGroupe>
            <Icon />
            <Icon />
            <Icon />
        </IconGroupe>
    );
};
